# Backend Production Environment Variables
# Add these to your Render dashboard

NODE_ENV=production
PORT=5001

# Generate a secure secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-minimum-32-characters-long

# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/financial_tracker?retryWrites=true&w=majority

# Frontend URL for CORS (your Vercel deployment URL)
CORS_ORIGIN=https://your-frontend-app.vercel.app
